---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<span
  class:list={[
    'inline-flex items-center gap-2 border border-border-strong px-3 py-1 font-mono text-xs uppercase tracking-wider text-signal',
    className,
  ]}
>
  <slot />
</span>
